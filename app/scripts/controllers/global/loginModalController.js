'use strict';

/**
 * @ngdoc function
 * @name appApp.controller:BoardBoardlistCtrl
 * @description
 * # loginModalController
 * Controller of the appApp
 */
angular.module('appApp').controller('loginModalController', function($scope) {

  $scope.close = function(result) {
    return close = "test";
    //  close(result);
  };
});
